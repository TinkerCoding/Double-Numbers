/**
* @param {array} numbers
*/
function doubleNumbers(numbers) {
   return numbers.map(function(number) {
       return number * 2
   })
}

console.log(doubleNumbers([10, 20]))
console.log(doubleNumbers([5, 12, 20, 10]))
